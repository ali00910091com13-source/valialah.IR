# راهنمای نصب روی هاست

## مرحله ۱: آپلود فایل‌ها

1. پوشه `dist` (خروجی بیلد) را به ریشه هاست آپلود کنید
2. پوشه `api` را به ریشه هاست آپلود کنید

## مرحله ۲: تنظیم دیتابیس

1. در cPanel یا DirectAdmin یک دیتابیس MySQL بسازید
2. نام دیتابیس، نام کاربری و رمز عبور را یادداشت کنید

## مرحله ۳: ویرایش فایل API

فایل `api/index.php` را باز کنید و مقادیر زیر را تغییر دهید:

```php
$DB_HOST = 'localhost';
$DB_NAME = 'نام_دیتابیس_شما';
$DB_USER = 'نام_کاربری_شما';
$DB_PASS = 'رمز_عبور_شما';
```

## مرحله ۴: تنظیم آدرس API

فایل `src/cloud.ts` را باز کنید و خط زیر را تغییر دهید:

```typescript
const API_BASE = "/api";
```

اگر سایت شما در ساب‌دامین یا ساب‌فولدر است:

```typescript
const API_BASE = "https://your-domain.com/api";
```

## مرحله ۵: بیلد و آپلود

```bash
npm run build
```

سپس محتویات پوشه `dist` را به هاست آپلود کنید.

## مرحله ۶: تست

1. به آدرس سایت بروید
2. وارد پنل مدیریت شوید: `yoursite.com/#/admin`
3. رمز عبور: `avayemehr`
4. یک پزشک یا مقاله اضافه کنید
5. سایت را در مرورگر دیگر باز کنید - باید تغییرات را ببینید

## عیب‌یابی

### خطای CORS
اگر خطای CORS دریافت کردید، فایل `api/index.php` را ویرایش کنید و خط زیر را اضافه کنید:

```php
header('Access-Control-Allow-Origin: *');
```

### خطای دیتابیس
اگر خطای اتصال به دیتابیس دریافت کردید:
- نام دیتابیس، نام کاربری و رمز عبور را بررسی کنید
- مطمئن شوید کاربر دیتابیس دسترسی کامل دارد

### خطای 404 برای API
اگر API کار نمی‌کند:
- مطمئن شوید پوشه `api` در ریشه هاست است
- فایل `.htaccess` را بررسی کنید
- mod_rewrite را در Apache فعال کنید

## ساختار فایل‌ها

```
public_html/
├── index.html
├── assets/
├── api/
│   ├── index.php
│   └── .htaccess
```

## پشتیبانی

اگر مشکلی داشتید، Console مرورگر (F12) را بررسی کنید و خطاها را گزارش دهید.
